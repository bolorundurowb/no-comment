name: Release extension

on:
  push:
    branches:
      - main
      - master
    paths:
      - "src/manifest.json"

permissions:
  contents: write

concurrency:
  group: extension-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect manifest version change
        id: meta
        run: |
          set -euo pipefail
          VERSION=$(jq -r '.version' src/manifest.json)
          echo "version=${VERSION}" >> "${GITHUB_OUTPUT}"

          if ! git rev-parse HEAD~1 >/dev/null 2>&1; then
            echo "changed=true" >> "${GITHUB_OUTPUT}"
            echo "No parent commit; releasing initial version ${VERSION}."
            exit 0
          fi

          PREV=""
          if git show HEAD~1:src/manifest.json > /tmp/previous-manifest.json 2>/dev/null; then
            PREV=$(jq -r '.version // empty' /tmp/previous-manifest.json)
          fi

          if [ "${PREV}" = "${VERSION}" ]; then
            echo "changed=false" >> "${GITHUB_OUTPUT}"
            echo "src/manifest.json version unchanged (${VERSION}); skipping tag and release."
          else
            echo "changed=true" >> "${GITHUB_OUTPUT}"
            echo "Version bump: ${PREV:-<none>} -> ${VERSION}"
          fi

      - name: Fetch tags
        if: steps.meta.outputs.changed == 'true'
        run: git fetch --tags origin

      - name: Skip release if tag already exists
        id: duplicate_tag
        if: steps.meta.outputs.changed == 'true'
        run: |
          set -euo pipefail
          VERSION="${{ steps.meta.outputs.version }}"
          SKIP=false
          if git show-ref --verify --quiet "refs/tags/v${VERSION}"; then
            SKIP=true
            echo "::notice::Local tag v${VERSION} already exists; skipping package and release."
          elif [ -n "$(git ls-remote --tags origin "refs/tags/v${VERSION}")" ]; then
            SKIP=true
            echo "::notice::Remote tag v${VERSION} already exists; skipping package and release."
          fi
          echo "skip_duplicate=${SKIP}" >> "${GITHUB_OUTPUT}"

      - name: Package extension zip
        if: steps.meta.outputs.changed == 'true' && steps.duplicate_tag.outputs.skip_duplicate != 'true'
        run: |
          set -euo pipefail
          VERSION="${{ steps.meta.outputs.version }}"
          mkdir -p dist
          ZIP="dist/no-comment-${VERSION}.zip"
          (cd src && zip -r "../${ZIP}" .)
          if [ -f LICENSE ]; then
            zip -r "${ZIP}" LICENSE
          fi
          unzip -l "${ZIP}"

      - name: Create GitHub release and tag
        if: steps.meta.outputs.changed == 'true' && steps.duplicate_tag.outputs.skip_duplicate != 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.meta.outputs.version }}
          name: No Comment v${{ steps.meta.outputs.version }}
          files: dist/no-comment-${{ steps.meta.outputs.version }}.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
